# The Recognition Threshold — Visual Asset Set

Generated by `transmission-engine` from `../recognition-threshold.md`. Last expanded 2026-05-25.

## What's here

### Post visuals (16:9, 1600×900)

| File (SVG + PNG) | Use in post |
|---|---|
| **`whole_pattern.svg` / `.png`** | **The compression. The entire article's pattern in one figure.** Two grounds at different elevations, threshold between, apparatus rooted on your ground, form arcing across, recognition lighting up on another ground. Use as header OR as the single canonical visual. |
| `header.svg` / `.png` | Alternate atmospheric header — luminous gold threshold on deep field, no text. Use if you want mood without explanation. |
| `pattern.svg` / `.png` + `.square.png` + `.card.png` | Threshold pattern (named sides only). Lighter than `whole_pattern`; use as inline visual after the opening if `whole_pattern` is the header. |
| `recognition_vs_validation.svg` / `.png` | Secondary pattern for the *recognition is not validation* section. Two luminous nodes held apart. |
| `capacity_vs_deposit.svg` / `.png` | Secondary pattern for the reframe section. Source emanating to four deposits. |

### Pull-quote share cards (1:1, 1080×1080)

| File | Quote |
|---|---|
| `quote_recognition_vs_validation.svg` / `.png` | *Validation closes a transaction. Recognition opens a use.* |
| `quote_lens_travels.svg` / `.png` | *The lens travels. The apparatus that built the lens stays where it was built.* |
| `quote_form_portable.svg` / `.png` | *The form is portable. The ground is yours.* |

### Teaching stack

| File | Use |
|---|---|
| `whole_pattern_annotated.svg` / `.png` (1800×1100) | Annotated version of the compression. Six numbered callouts with a reading legend. Use when introducing the figure to someone seeing it cold — workshop slides, kit documentation, first-time-reader contexts. |
| `whole_pattern_step1.svg` → `step5.svg` (1600×900 each) | Five-frame sequential reveal. Each frame adds one element. Use as a Twitter/X/LinkedIn thread, as slides in a workshop, or as click-through teaching content. Frame titles: *Terrain → Apparatus → Form → Recognition → Propagation.* |
| `whole_pattern_worksheet.svg` / `.png` (1240×1600, portrait) | Practitioner-fillable version. Empty slots for the reader's own apparatus, form, grounds, and recognition test. Print or use in a PDF annotator. Built for kit / workshop / Genesis Seed use. |

### Reference / engine output

| File | Purpose |
|---|---|
| `preview.html` | Open in browser to see all visuals at full size. |
| `header-prompt.md` | Imagen 4 prompt — for an alternative painterly/photographic header if the SVG header reads too geometric. |
| `pull-quotes.md` | All five pull-quotes as markdown text (for use as social caption / link copy). |
| `read-notes.md` | Engine's structural read of the article. |

## Recommended publication sequence

1. **Substack post (recommended sequence):**
   - Header: upload `whole_pattern.svg` — the article in one figure, sets the entire frame before the reader's first paragraph
   - Body: paste article from `../recognition-threshold.md`
   - Inline visual 1: `recognition_vs_validation.svg` in the *recognition is not validation* section
   - Inline visual 2: `capacity_vs_deposit.svg` in the reframe section
   - (Optional alt header: `header.svg` if you want pure mood without text)

2. **Launch day social posts (one per day, in order):**
   - Day of publication: post `quote_form_portable.png` with link
   - Day +1: post `quote_recognition_vs_validation.png` with link
   - Day +3: post `quote_lens_travels.png` with link

3. **(Optional)** If `header.svg` reads too geometric for the post you're publishing, run `header-prompt.md` through Gemini → Imagen 4, save the chosen generation as `header.imagen.png`, use that instead.

## Format guidance

- **Substack** accepts SVG on upload and converts. Use SVG for highest fidelity.
- **X/Twitter, LinkedIn, Instagram** require raster. Use the `.png` versions.
- **Cross-platform sharing of the primary pattern:** the `.square.png` (1080×1080) and `.card.png` (1200×675) variants are pre-generated for IG/LinkedIn and X cards respectively.
